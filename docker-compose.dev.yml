# Dev overlay: hot-reload enabled
# Usage: docker compose -f docker-compose.yml -f docker-compose.dev.yml up
version: "3.9"

services:
  api:
    target: development
    volumes:
      - ./packages/api/src:/app/src:rw
      - ./packages/shared/src:/app/shared:rw
